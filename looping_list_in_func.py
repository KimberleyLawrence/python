n = [3, 5, 7]

def total(numbers):
    result = 0
    for number in numbers:
        # adds each number into result. This will continue through the list, index 0 will =  3, then index 1 will = 8, then index 2 will = 15
        result= result + number 
    #return result
    return result
#return numbers[result]
